language: node_js
os: linux
sudo: false
node_js:
- '10'
addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable

cache:
  directories:
     - ./node_modules

install:
  - npm install

script:
  - build-all.sh

